{{!< default}}
{{#post}}
  <article class="gh-article {{post_class}}">
    <header class="gh-header gh-canvas">
      <h1 class="gh-title">{{title}}</h1>
      <span class="gh-post-meta">
        <time datetime="{{date format="YYYY-MM-DD"}}">{{date format="YYYY-MM-DD"}}</time>
        <span class="gh-post-authors">
          &middot; {{#foreach authors}}<a href="{{url}}">{{name}}</a>{{#unless @last}},&nbsp;{{/unless}}{{/foreach}}
        </span>
      </span>
      {{#if feature_image}}
        <img class="gh-feature-image" src="{{feature_image}}" alt="{{title}}" />
      {{/if}}
    </header>
    <div class="gh-content gh-canvas">
      {{content}}
    </div>
    <footer class="gh-footer gh-canvas">
      <div class="gh-post-tags">
        {{#foreach tags}}<a class="tag" href="{{url}}">{{name}}</a>{{/foreach}}
      </div>
      <a href="#" onclick="window.scrollTo(0, 0); return false;" style="text-align: center; margin-top: 1em">
        &uarr; TOP
      </a>
    </footer>
  </article>
{{/post}}